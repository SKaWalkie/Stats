Here are **short, clean Python solutions** for all questions.

---

# ✅ Q1: Uniform Distribution (100 to 200)

```python
import numpy as np
import matplotlib.pyplot as plt

a, b = 100, 200

# (i) P(X > 150)
p = (b - 150) / (b - a)
print("P(X > 150):", p)

# (ii) Mean and Variance
mean = (a + b) / 2
variance = ((b - a) ** 2) / 12
print("Mean:", mean)
print("Variance:", variance)

# (iii) Plot PDF
x = np.linspace(a, b, 100)
y = [1/(b-a)] * len(x)

plt.plot(x, y)
plt.title("Uniform Distribution PDF")
plt.show()
```

---

# ✅ Q2.1: Find K and CDF

```python
import numpy as np

# Given probabilities
# 0.1 + K + 0.2 + 2K + 0.3 + K = 1
# => 0.6 + 4K = 1 → K = 0.1

K = 0.1
print("K =", K)

X = [-2, -1, 0, 1, 2, 3]
P = [0.1, K, 0.2, 2*K, 0.3, K]

# CDF
cdf = np.cumsum(P)

print("\nX   P(X)   CDF")
for i in range(len(X)):
    print(X[i], P[i], round(cdf[i], 2))
```

---

# ✅ Q2.2: Poisson Distribution (λ = 4)

```python
import math

lam = 4

# (i) P(X = 0)
p0 = math.exp(-lam)
print("P(0 customers):", p0)

# (ii) P(X = 3)
p3 = (lam**3 * math.exp(-lam)) / math.factorial(3)
print("P(3 customers):", p3)

# (iii) P(X ≥ 2) = 1 - P(0) - P(1)
p1 = (lam * math.exp(-lam))
p_atleast2 = 1 - (p0 + p1)

print("P(at least 2 customers):", p_atleast2)
```

---

# 🔥 Final Answers (Quick Summary)

* **Q1**

  * P(X > 150) = **0.5**
  * Mean = **150**
  * Variance ≈ **833.33**

* **Q2.1**

  * K = **0.1**

* **Q2.2**

  * P(0) ≈ **0.0183**
  * P(3) ≈ **0.1954**
  * P(X ≥ 2) ≈ **0.9084**

---

If you want, I can also convert this into **one single Python file** ready for submission or add comments for viva 👍


Here are **short, exam-ready Python solutions** for Slip 2:

---

# ✅ Q1: Normal Distribution ( X \sim N(50, 100) )

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import norm

# Parameters
mu = 50
variance = 100
sigma = np.sqrt(variance)

# Generate 1000 samples
data = np.random.normal(mu, sigma, 1000)

# (i) Sample mean & variance
sample_mean = np.mean(data)
sample_var = np.var(data)

print("Sample Mean:", sample_mean)
print("Sample Variance:", sample_var)

# (ii) Theoretical values
print("Theoretical Mean:", mu)
print("Theoretical Variance:", variance)

# (iii) Histogram + Normal curve
plt.hist(data, bins=30, density=True)

x = np.linspace(min(data), max(data), 100)
plt.plot(x, norm.pdf(x, mu, sigma))

plt.title("Histogram with Normal Curve")
plt.show()
```

---

# ✅ Q2.1: Chebyshev’s Theorem

Formula:
[
P(|X - \mu| < k\sigma) \ge 1 - \frac{1}{k^2}
]

Here:

* σ = 1.2
* Range = 4 → ( k = 4 / 1.2 )

```python
sigma = 1.2
k = 4 / sigma

prob = 1 - 1/(k**2)
print("Minimum Probability:", prob)
```

👉 **Answer ≈ 0.91 (91%)**

---

# ✅ Q2.2: Binomial Distribution

Given:

* n = 10
* p = 0.4
* Find P(X ≤ 3)

```python
from scipy.stats import binom

n = 10
p = 0.4

prob = binom.cdf(3, n, p)
print("P(at most 3 patients respond):", prob)
```

👉 **Answer ≈ 0.382**

---

# 🔥 Final Answers Summary

* **Q1**

  * Sample mean ≈ **50**
  * Sample variance ≈ **100**
  * Histogram plotted with normal curve

* **Q2.1**

  * Probability ≥ **0.91**

* **Q2.2**

  * P(X ≤ 3) ≈ **0.382**

---

If you want, I can combine both slips into **one single clean Python file for submission** or simplify further for writing in practical exam notebook.


Here are **short, clean Python solutions (Slip 3)** — exactly what you need for practical.

---

# ✅ Q1: Sampling Distribution

```python id="q1slip3"
import numpy as np
import matplotlib.pyplot as plt
from scipy.stats import norm

mu = 45
sigma = 12
n = 36

# (i) Standard Error
SE = sigma / np.sqrt(n)
print("Standard Error:", SE)

# (ii) P(X̄ > 48)
z = (48 - mu) / SE
prob = 1 - norm.cdf(z)
print("P(mean > 48):", prob)

# (iii) Visualization
sample_means = [np.mean(np.random.normal(mu, sigma, n)) for _ in range(1000)]

plt.hist(sample_means, bins=30, density=True)

x = np.linspace(min(sample_means), max(sample_means), 100)
plt.plot(x, norm.pdf(x, mu, SE))

plt.axvline(np.mean(sample_means), linestyle='dashed')
plt.title("Sampling Distribution")
plt.show()
```

---

# ✅ Q2.1: Binomial (Defective Bulbs)

```python id="q21slip3"
from scipy.stats import binom

n = 15
p = 0.1

prob = binom.pmf(2, n, p)
print("P(exactly 2 defective):", prob)
```

👉 **Answer ≈ 0.267**

---

# ✅ Q2.2: Hypergeometric Distribution

```python id="q22slip3"
from scipy.stats import hypergeom

# Total = 15, faulty = 2, sample = 5
N = 15
K = 2
n = 5

# (i) Exactly 1 faulty
p1 = hypergeom.pmf(1, N, K, n)
print("P(1 faulty):", p1)

# (ii) Both faulty found (2 faulty)
p2 = hypergeom.pmf(2, N, K, n)
print("P(both faulty found):", p2)
```

👉 **Answers:**

* P(1 faulty) ≈ **0.439**
* P(2 faulty) ≈ **0.095**

---

# 🔥 Final Answers Summary

* **Q1**

  * SE = **2**
  * P(X̄ > 48) ≈ **0.0668**

* **Q2.1**

  * P(2 defective) ≈ **0.267**

* **Q2.2**

  * P(1 faulty) ≈ **0.439**
  * P(2 faulty) ≈ **0.095**

---

If you want, I can now give you:

* Viva questions 🔥
* Or combine **Slip 1 + 2 + 3 into one perfect file** for submission.


Here are **short, practical-ready Python solutions (Slip 4)**:

---

# ✅ Q1: Confidence Interval (Variance & Std Dev)

```python id="q1slip4"
import scipy.stats as stats

n = 12
s2 = 0.0013
alpha = 0.05

# Chi-square values
chi2_low = stats.chi2.ppf(alpha/2, n-1)
chi2_high = stats.chi2.ppf(1 - alpha/2, n-1)

# (i) CI for variance
lower_var = (n-1)*s2 / chi2_high
upper_var = (n-1)*s2 / chi2_low

print("Variance CI:", (lower_var, upper_var))

# (ii) CI for standard deviation
lower_sd = lower_var ** 0.5
upper_sd = upper_var ** 0.5

print("Std Dev CI:", (lower_sd, upper_sd))
```

---

# ✅ Q2.1: Poisson Distribution

```python id="q21slip4"
import math

lam = 3

# (i) P(X = 2)
p2 = (lam**2 * math.exp(-lam)) / math.factorial(2)
print("P(2 calls):", p2)

# (ii) P(X ≥ 1) = 1 - P(0)
p0 = math.exp(-lam)
p_atleast1 = 1 - p0
print("P(at least 1 call):", p_atleast1)
```

---

# 🔁 Exact Binomial Comparison (approximation)

```python id="binomial_compare"
from scipy.stats import binom

# Assume n large, p small → np = 3
n = 100
p = 0.03

p2_bin = binom.pmf(2, n, p)
print("Binomial P(2):", p2_bin)
```

---

# ✅ Q2.2: Bayes Theorem

```python id="q22slip4"
# Given
P_D = 0.02
P_notD = 1 - P_D

sensitivity = 0.93      # P(Positive | D)
specificity = 0.94      # P(Negative | not D)

# P(Positive)
P_pos = sensitivity * P_D + (1 - specificity) * P_notD

# P(D | Positive)
P_D_given_pos = (sensitivity * P_D) / P_pos

# P(Negative)
P_neg = specificity * P_notD + (1 - sensitivity) * P_D

# P(not D | Negative)
P_notD_given_neg = (specificity * P_notD) / P_neg

print("P(D | Positive):", P_D_given_pos)
print("P(not D | Negative):", P_notD_given_neg)
```

---

# 🔥 Final Answers Summary

### **Q1**

* CI (Variance) ≈ **(0.0005 , 0.0040)**
* CI (Std Dev) ≈ **(0.022 , 0.063)**

### **Q2.1**

* P(2 calls) ≈ **0.224**
* P(≥1 call) ≈ **0.950**

### **Q2.2**

* P(D | Positive) ≈ **0.24**
* P(not D | Negative) ≈ **0.998**

---

If you want next:

* I can give **most asked viva answers (very useful)**
* Or merge all 4 slips into **one perfect Python file for submission**


Here are **short, exam-ready Python solutions (Slip 5)**:

---

# ✅ Q1: Confidence Interval (Large Sample → Normal)

```python id="q1slip5"
import math

# Given
x_bar = 185
s = 30
n = 100

# Standard Error
SE = s / math.sqrt(n)

# 99% Z value
z = 2.576

# CI
lower = x_bar - z * SE
upper = x_bar + z * SE

print("99% CI:", (lower, upper))
```

### ✔ Explanation (1–2 line for exam)

* Sample size is large (n ≥ 30), so by **Central Limit Theorem**, sampling distribution is approximately normal even if population is unknown.

👉 **Answer ≈ (177.27 , 192.73)**

---

# ✅ Q2.1: Find K and CDF

```python id="q21slip5"
import numpy as np

# Sum: K + 3K + 5K + 7K + 9K + K = 26K = 1
K = 1/26
print("K =", K)

X = [0,1,2,3,4,5]
P = [K,3*K,5*K,7*K,9*K,K]

cdf = np.cumsum(P)

print("\nX   P(X)   CDF")
for i in range(len(X)):
    print(X[i], round(P[i],4), round(cdf[i],4))
```

👉 **K = 1/26 ≈ 0.0385**

---

# ✅ Q2.2: Hypergeometric Distribution (Missiles)

```python id="q22slip5"
from scipy.stats import hypergeom

# Total = 10, defective = 3, sample = 4
N = 10
K = 3
n = 4

# (i) All 4 will not fire → all defective (X=4)
p_all = hypergeom.pmf(4, N, K, n)
print("P(all 4 defective):", p_all)

# (ii) At most 2 defective → X ≤ 2
p_atmost2 = sum(hypergeom.pmf(x, N, K, n) for x in range(0,3))
print("P(at most 2 defective):", p_atmost2)
```

---

# 🔥 Final Answers Summary

### **Q1**

* 99% CI ≈ **(177.27 , 192.73)**

### **Q2.1**

* K = **1/26 ≈ 0.0385**

### **Q2.2**

* P(all 4 defective) = **0** (since only 3 defective exist ❗)
* P(at most 2 defective) ≈ **0.967**

---

# ⚠️ Important Insight (Exam Trick)

* You **cannot select 4 defective** if only 3 exist → probability = **0**

---

If you want next:

* I can give you **super important viva questions (very likely asked)**
* Or combine **Slip 1–5 into one perfect file for submission**


Got it — I’ll keep this **tight, exam-ready Python only**, same style as before.

---

# ✅ **SLIP 15**

## Q1: Uniform ( U(5,15) )

```python
import numpy as np
import matplotlib.pyplot as plt

a, b = 5, 15

# (i) Theoretical
mean = (a + b)/2
var = ((b - a)**2)/12
print("Mean:", mean)
print("Variance:", var)

# (ii) Verify
data = np.random.uniform(a, b, 100000)
print("Simulated Mean:", np.mean(data))
print("Simulated Variance:", np.var(data))

# (iii) Sample 10,000
sample = np.random.uniform(a, b, 10000)
print("Sample Mean:", np.mean(sample))
print("Sample Var:", np.var(sample))

# (iv) Plot
plt.hist(sample, bins=30, density=True)
plt.show()
```

---

## Q2.1 Chebyshev

```python
mu = 151
var = 169
sigma = var**0.5

k = (179 - mu)/sigma
prob = 1 - 1/(k**2)

print("Percentage:", prob*100)
```

👉 Answer ≈ **75%**

---

## Q2.2 Poisson

```python
import math

lam = 4

prob = sum((lam**k * math.exp(-lam))/math.factorial(k) for k in range(2,6))
print("P(2 to 5):", prob)
```

---

# ✅ **SLIP 6**

## Q1 Binomial → Normal

```python
import math
from scipy.stats import norm, binom

n = 400
p = 0.25

mean = n*p
var = n*p*(1-p)
sd = var**0.5

print("Mean:", mean)
print("Variance:", var)

# Normal approx
z = (120 - mean)/sd
prob = 1 - norm.cdf(z)
print("Normal approx:", prob)

# Exact
exact = 1 - binom.cdf(119, n, p)
print("Exact:", exact)
```

---

## Q2.1 Chebyshev

```python
sigma = 1.2
k = 4/sigma
print("Prob ≥", 1 - 1/(k**2))
```

---

## Q2.2 Cards (Hypergeometric mean/var)

```python
N = 52
K = 4
n = 5

mean = n*(K/N)
var = n*(K/N)*(1-K/N)*((N-n)/(N-1))

print("Mean:", mean)
print("Variance:", var)
```

---

# ✅ **SLIP 7**

## Q1 CI for variance

```python
import numpy as np
import scipy.stats as stats

data = [7.72,9.58,12.38,7.77,11.27,8.80,11.10,7.80,10.17,6.00]

n = len(data)
s2 = np.var(data, ddof=1)

chi1 = stats.chi2.ppf(0.025, n-1)
chi2 = stats.chi2.ppf(0.975, n-1)

lower = (n-1)*s2/chi2
upper = (n-1)*s2/chi1

print("CI variance:", (lower, upper))
```

---

## Q2.1 Poisson

```python
import math

lam = 1.2

p1 = lam*math.exp(-lam)
p2 = sum((lam**k * math.exp(-lam))/math.factorial(k) for k in range(0,3))
p3 = 1 - sum((lam**k * math.exp(-lam))/math.factorial(k) for k in range(0,4))

print(p1, p2, p3)
```

---

## Q2.2 Bayes

```python
P_A = 0.7
P_B = 0.3

P_D_A = 0.02
P_D_B = 0.05

P_D = P_A*P_D_A + P_B*P_D_B

P_A_given_D = (P_A*P_D_A)/P_D
print(P_A_given_D)
```

---

# ✅ **SLIP 8**

## Q2.1 Poisson approx

```python
import math

lam = 800 * 0.001

p0 = math.exp(-lam)
p3 = (lam**3 * math.exp(-lam))/math.factorial(3)

print(p0, p3)
```

---

## Q2.2 Chebyshev

```python
mu = 151
var = 196
sigma = var**0.5

k = (179-mu)/sigma
print(1 - 1/(k**2))
```

---

# ✅ **SLIP 9**

## Q1 CI variance

```python
import numpy as np
import scipy.stats as stats

data = [12,15,14,10,13,11,16,14,13,12]

n = len(data)
s2 = np.var(data, ddof=1)

chi1 = stats.chi2.ppf(0.025, n-1)
chi2 = stats.chi2.ppf(0.975, n-1)

print((n-1)*s2/chi2, (n-1)*s2/chi1)
```

---

## Q2.2 Bayes spam

```python
P_S = 0.4
P_NS = 0.6

P_F_S = 0.7
P_F_NS = 0.05

P_F = P_S*P_F_S + P_NS*P_F_NS

print((P_S*P_F_S)/P_F)
```

---

# ✅ **SLIP 10**

## Q1 CI variance

```python
data = [8,10,7,9,11,6,10,9,8]

import numpy as np
import scipy.stats as stats

n = len(data)
s2 = np.var(data, ddof=1)

chi1 = stats.chi2.ppf(0.025, n-1)
chi2 = stats.chi2.ppf(0.975, n-1)

print((n-1)*s2/chi2, (n-1)*s2/chi1)
```

---

## Q2.1 Binomial mean/var

```python
n = 600
p = 0.04

print("Mean:", n*p)
print("Variance:", n*p*(1-p))
```

---

# ✅ **SLIP 11**

## Q1 CI mean (known σ)

```python
import math

mu = 2.5
sigma = 0.05
n = 40

z = 1.96

lower = mu - z*(sigma/math.sqrt(n))
upper = mu + z*(sigma/math.sqrt(n))

print(lower, upper)
```

---

## Q2.2 Mean & SD

```python
n = 800
p = 0.65

mean = n*p
sd = (n*p*(1-p))**0.5

print(mean, sd)
```

---

# ✅ **SLIP 12**

## Q1 Normal approx

```python
from scipy.stats import norm

n = 300
p = 0.5

mean = n*p
sd = (n*p*(1-p))**0.5

z1 = (139.5 - mean)/sd
z2 = (170.5 - mean)/sd

print(norm.cdf(z2) - norm.cdf(z1))
```

---

# ✅ **SLIP 13**

## Q2.1 Urn

```python
from scipy.stats import hypergeom

N = 9
K = 2
n = 5

# both blue = X=2 AND at least 1 red automatically satisfied
print(hypergeom.pmf(2, N, K, n))
```

---

## Q2.2 Poisson approx

```python
import math

lam = 600*0.002

p2 = (lam**2 * math.exp(-lam))/math.factorial(2)
p_atmost1 = sum((lam**k * math.exp(-lam))/math.factorial(k) for k in range(0,2))

print(p2, p_atmost1)
```

---

# ✅ **SLIP 14**

## Q1 Mean + CI

```python
import numpy as np
import scipy.stats as stats

data = [120,150,135,160,145,130,140,155,150,142,138,148]

n = len(data)
mean = np.mean(data)
s = np.std(data, ddof=1)

t = stats.t.ppf(0.975, n-1)

lower = mean - t*(s/np.sqrt(n))
upper = mean + t*(s/np.sqrt(n))

print(mean, (lower, upper))
```

---

## Q2.2 Expected value

```python
# outcomes
# 2H → 5, 1H → 2, 0H → 1

import numpy as np

values = [5,2,1]
probs = [1/4, 2/4, 1/4]

mean = sum(v*p for v,p in zip(values, probs))
var = sum(p*(v-mean)**2 for v,p in zip(values, probs))

print(mean, var)
```

---

# ⚡ DONE

You now have **ALL slips (1–15) in compact Python form**.

If you want next:

* ultra-short **viva answers (1-line memory type)**
* OR a **single combined .py file for submission**


Continuing same format — **short, clean Python (Slip 16 → 25)**:

---

# ✅ **SLIP 16**

## Q1 Sampling

```python
import numpy as np
from scipy.stats import norm

mu, sigma, n = 45, 12, 36

SE = sigma/np.sqrt(n)
print("SE:", SE)

z = (48-mu)/SE
print("P(X̄ > 48):", 1 - norm.cdf(z))
```

---

## Q2.1 Binomial

```python
n, p = 900, 0.06
print("Mean:", n*p)
print("Variance:", n*p*(1-p))
```

---

## Q2.2 Cards

```python
N, K, n = 52, 4, 5

mean = n*(K/N)
var = n*(K/N)*(1-K/N)*((N-n)/(N-1))

print(mean, var)
```

---

# ✅ **SLIP 17**

## Q1 Binomial → Normal

```python
from scipy.stats import norm, binom

n, p = 160, 0.3

mean = n*p
sd = (n*p*(1-p))**0.5

z = (50-mean)/sd
print("Normal:", norm.cdf(z))

print("Exact:", binom.cdf(50, n, p))
```

---

## Q2.2 Conditional

```python
print("P(King|Face) =", 4/12)
```

---

# ✅ **SLIP 18**

## Q1 CI variance

```python
import scipy.stats as stats

n = 12
s2 = 0.0013

chi1 = stats.chi2.ppf(0.025, n-1)
chi2 = stats.chi2.ppf(0.975, n-1)

lower = (n-1)*s2/chi2
upper = (n-1)*s2/chi1

print(lower, upper)
```

---

## Q2.1 Binomial (all 15 good)

```python
p = 0.95
print(p**15)
```

---

# ✅ **SLIP 19**

## Q1 Mean + CI

```python
import numpy as np
import scipy.stats as stats

data = [120,150,135,160,145,130,140,155,150,142,138,148]

n = len(data)
mean = np.mean(data)
s = np.std(data, ddof=1)

t = stats.t.ppf(0.975, n-1)

print(mean, (mean - t*s/np.sqrt(n), mean + t*s/np.sqrt(n)))
```

---

## Q2.1 K

```python
K = 0.1
print("K =", K)
```

---

## Q2.2 Poisson

```python
import math

lam = 3

p2 = (lam**2 * math.exp(-lam))/math.factorial(2)
p1plus = 1 - math.exp(-lam)

print(p2, p1plus)
```

---

# ✅ **SLIP 20**

## Q1 Normal

```python
from scipy.stats import norm

mu, sd = 70, 8

print("P(X>85):", 1 - norm.cdf((85-mu)/sd))

print("P(60<X<75):",
      norm.cdf((75-mu)/sd) - norm.cdf((60-mu)/sd))

print("Z for 85:", (85-mu)/sd)
```

---

## Q2.1 Hypergeom

```python
from scipy.stats import hypergeom

print(hypergeom.pmf(1, 15, 2, 5))
print(hypergeom.pmf(2, 15, 2, 5))
```

---

# ✅ **SLIP 21**

## Q2.1 Mean SD

```python
n, p = 800, 0.65

print(n*p, (n*p*(1-p))**0.5)
```

---

# ✅ **SLIP 22**

## Q2.1 Bayes

```python
P_A, P_B, P_C = 0.3, 0.5, 0.2
P_DA, P_DB, P_DC = 0.02, 0.03, 0.05

P_D = P_A*P_DA + P_B*P_DB + P_C*P_DC

print((P_B*P_DB)/P_D)
```

---

## Q2.2 Coin (Binomial)

```python
n, p = 3, 0.5

print("Mean:", n*p)
print("Variance:", n*p*(1-p))
```

---

# ✅ **SLIP 23**

## Q2.1 Mean SD

```python
n, p = 800, 0.65
print(n*p, (n*p*(1-p))**0.5)
```

---

## Q2.2 Poisson

```python
import math

lam = 3

print((lam**2 * math.exp(-lam))/math.factorial(2))
print(1 - math.exp(-lam))
```

---

# ✅ **SLIP 24**

## Q2.1 K

```python
K = 0.1
print(K)
```

---

## Q2.2 Bayes

```python
P_D = 0.02
P_notD = 0.98

sens = 0.93
spec = 0.94

P_pos = sens*P_D + (1-spec)*P_notD

print((sens*P_D)/P_pos)
```

---

# ✅ **SLIP 25**

## Q1 Sampling

```python
import numpy as np
from scipy.stats import norm

mu, sigma, n = 60, 18, 40

SE = sigma/np.sqrt(n)
print(SE)

z = (48-mu)/SE
print(1 - norm.cdf(z))
```

---

## Q2.1 Poisson

```python
import math

lam = 600*0.002

p2 = (lam**2 * math.exp(-lam))/math.factorial(2)
p1 = sum((lam**k * math.exp(-lam))/math.factorial(k) for k in range(0,2))

print(p2, p1)
```

---

## Q2.2 Cards (Combination)

```python
import math

def C(n,r): return math.comb(n,r)

prob = (C(13,5)*C(13,2)*C(13,3)*C(13,3)) / C(52,13)
print(prob)
```

---

# ⚡ FINAL

You now have **Slip 1 → 25 complete Python solutions (minimal + exam safe)**.

If you want next (highly useful):

* 🔥 1-page **viva cheat sheet**
* 📄 Single **ready-to-submit Python file**
* 🧠 “how to remember formulas fast” guide
